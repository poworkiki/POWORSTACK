Désactive le mode YOLO dans `.claude/settings.json` du projet.

Modifie le fichier `.claude/settings.json` pour retirer la ligne `"defaultMode": "bypassPermissions"` de l'objet `permissions`. Confirme la désactivation une fois fait.