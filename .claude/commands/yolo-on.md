Active le mode YOLO (bypassPermissions) dans `.claude/settings.json` du projet.

Modifie le fichier `.claude/settings.json` pour ajouter ou mettre à jour `"defaultMode": "bypassPermissions"` dans l'objet `permissions`. Si la clé existe déjà, remplace sa valeur. Confirme l'activation une fois fait.